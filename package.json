{
  "name": "student-life-os",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Integrated adaptive student life planner. Built by Rajkamal.",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server/local.js",
    "dev": "node --watch --env-file-if-exists=.env server/local.js",
    "build": "node scripts/build.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "pg": "8.16.3"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
